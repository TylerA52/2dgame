/*
#include <vector>
#include <SDL.h> //fix this

class Tilemap {

public:
    Tilemap(int width, int height, int tileWidth, int tileHeight, Renderer* renderer);
    ~Tilemap();

    void loadTilemap(const std::string& filename);
    void renderTilemap();
    void setTile(int x, int y, int tileIndex);
                                    
private:
    int mapWidth, mapHeight;
    int tileWidth, tileHeight
    std::vectory<std::vector<int>> tiles; // stores tile indices in the grid
    std::vector<SDL_Texture*> tileTextures; // list of tile textures
    Renderer* renderer; //reference to the renderer class for drawing textures

};

*/
